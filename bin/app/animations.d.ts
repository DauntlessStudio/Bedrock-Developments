export declare function createNewAnimation(names: string[], loop: boolean, commands: string, time: number): Promise<void>;
export declare function createNewController(names: string[], entry: string, exit: string | undefined, anim: string | undefined, query: string | undefined, transition: string): Promise<void>;
